import LandingPage, { landingMetadata } from '../_seo/landing-page';

export const metadata = landingMetadata('auphonic-alternative');

export default function Page() {
  return <LandingPage slug="auphonic-alternative" />;
}
