import LandingPage, { landingMetadata } from '../_seo/landing-page';

export const metadata = landingMetadata('adobe-podcast-alternative');

export default function Page() {
  return <LandingPage slug="adobe-podcast-alternative" />;
}
