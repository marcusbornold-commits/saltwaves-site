import LandingPage, { landingMetadata } from '../_seo/landing-page';

export const metadata = landingMetadata('podcast-audio-cleanup');

export default function Page() {
  return <LandingPage slug="podcast-audio-cleanup" />;
}
