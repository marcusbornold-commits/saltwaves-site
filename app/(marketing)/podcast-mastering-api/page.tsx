import LandingPage, { landingMetadata } from '../_seo/landing-page';

export const metadata = landingMetadata('podcast-mastering-api');

export default function Page() {
  return <LandingPage slug="podcast-mastering-api" />;
}
